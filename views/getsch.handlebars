{{#each result}}
<li>
    <a href="/confirmresv?rid={{../rid}}&from={{../from}}&to={{../to}}&schid={{id}}">
    <h3>{{time}}</h3><span class="{{color}} ui-li-count">Seats Left: {{seatsleft}}</span>
    </a>
</li>
{{/each}}